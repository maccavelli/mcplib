package mcplib

// Shared string constants for env checks and JSON-schema field names.
const (
	EnvValueTrue     = "true"
	SchemaTypeObject = "object"
	SchemaTypeKey    = "type"
	FieldSessionID   = "session_id"
	FieldProjectID   = "project_id"
)
